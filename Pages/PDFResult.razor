@page "/result/{FileName}"
@inherits PDFResult

@if (!string.IsNullOrWhiteSpace(ViewerUrl))
{
    <iframe src="@ViewerUrl"
            title="PDF Viewer"
            style="width:100%;height:80vh;border:none;"></iframe>
}
else
{
    <div class="text-danger">No PDF to display.</div>
}
